name: Learning Workflow

on:
  push:

jobs:
  learning:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "Starting workflow"

      - name: Show Date
        run: date

      - name: Show Directory
        run: pwd

      - name: Finish
        run: echo "Workflow completed"
